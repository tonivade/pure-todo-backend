apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

jar {
  baseName = 'todo-backend'
  version = '0.1.0-SNAPSHOT'
}

sourceCompatibility = 14
targetCompatibility = 14

repositories {
  mavenLocal()
  mavenCentral()
  maven {
    url = 'https://oss.sonatype.org/content/repositories/snapshots'
  }
}

tasks.withType(JavaCompile).each {
  it.options.compilerArgs.addAll('--enable-preview', '--release', '14')
}

test {
  useJUnitPlatform()

  jvmArgs(['--enable-preview'])
}

dependencies {
  compile 'com.github.tonivade:purefun-monad:1.8'
  compile 'com.github.tonivade:purefun-effect:1.8'
  compile 'com.github.tonivade:purefun-instances:1.8'
  compile 'com.github.tonivade:zeromock-server:0.13.0-SNAPSHOT'
  compile 'com.github.tonivade:zeromock-pure:0.13.0-SNAPSHOT'
  compile 'com.github.tonivade:puredbc:0.1.0-SNAPSHOT'
  compile 'com.github.tonivade:purecfg:0.1.0-SNAPSHOT'
  compile 'com.google.code.gson:gson:2.8.5'
  compile 'com.zaxxer:HikariCP:3.4.2'
  compile 'org.flywaydb:flyway-core:5.2.4'
  runtime 'com.h2database:h2:1.4.200'
  testCompile 'com.github.tonivade:zeromock-junit5:0.13.0-SNAPSHOT'
  testCompile 'org.junit.jupiter:junit-jupiter-engine:5.6.2'
  testCompile 'org.junit.platform:junit-platform-runner:1.6.2'
  testCompile 'org.mockito:mockito-core:3.3.3'
  testCompile 'org.mockito:mockito-junit-jupiter:3.3.3'
}

application {
  mainClassName = 'com.github.tonivade.todo.Application'
}

task fatJar(type: Jar) {
  manifest {
    attributes "Main-Class": "com.github.tonivade.todo.Application"
  }
  classifier = "all"
  from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    configurations.runtime.collect { it.isDirectory() ? it : zipTree(it) }
  }
  with jar
}
